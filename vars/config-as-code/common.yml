---
# password should be vault encrypted using e.g. ansible-vault encrypt_string '<password>'. Make a note of your vault password, as you'll need to pass it in when prompted while running the playbook, via --ask-vault-pass
aap_hostname: <your AAP hostname>
aap_username: admin
aap_password: <password> # TODO

# proxy_url: url
# proxy_username: user 
# proxy_password: password


# remote_exec_ansible_user: ansible
# remote_exec_ansible_password: <password> # TODO
